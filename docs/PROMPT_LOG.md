# Prompt Log — Meridian Financial

Every prompt given to Claude Code, timestamped, with a one-line note on what it produced and
whether it worked first try. (Required deliverable, brief §18.)

| # | Date (2026) | Prompt (condensed) | Produced | First try? |
|---|---|---|---|---|
| 1 | Aug 9 | Attached `CLAUDE_CODE_BRIEF.md`: "Read the brief in full, then produce `docs/BUILD_PLAN.md` before writing any code. New project, stored completely separately in GitHub and cloud container." | Full build plan (`docs/BUILD_PLAN.md`), decisions log seeded (D-000…D-006), this log, time log skeleton. Answered all four §20 open questions and flagged 6 internal inconsistencies in the brief. | Mostly — the plan itself yes; creating the separate GitHub repo was blocked by integration permissions (403), so work is parked on an orphan branch pending manual repo creation. |
| 2 | Aug 9 | "I opened another repo called meridian" | Migrated all project files from the GridPilot-AI parking branch into `darrshen97-ui/meridian` (now the permanent home); deleted the parking branch; updated D-000 and this log. | Yes. |
| 3 | Aug 9 | "Build" | Milestones 1–3: FastAPI scaffold + `/health`; all 14 tables as SQLAlchemy models with reversible Alembic migration; Argon2id auth with JWT cookie; user-scoped repositories; 12 pytest tests passing including the profile-isolation proof and a structural test that every repository method requires `user_id`. Vite/React/Tailwind scaffold builds into `app/static` and is served by the backend. | Yes — one hiccup (`alembic init` refused a pre-created directory; wrote the config by hand). |
| 4 | Aug 9 | "Build" (milestone 4) | Deterministic mock dataset generator: 2,278 + 1,073 transactions, 79 PDFs in three deliberately different layouts, 26 CSVs with real-world quirks, 12 OFX files, provider fixtures with the Aug 1–9 tail, all 13 planted events, DATASET_GUIDE.md. Byte-identical regeneration verified; 23 new tests. | Nearly — first run's volumes were under target (1,880/878), fixed by densifying discretionary spending; `••` masks broke PDF text extraction, switched to ASCII masks (D-010). |
| 5 | Aug 9 | "build" (milestone 5) | StatementParser protocol + registry with 8 parsers (3 PDF layouts, 4 CSV platforms, OFX); two-layer dedupe matcher shared with future reconciliation; ingestion service with upload → preview → confirm → import over HTTP; account inference from masked numbers with ask-don't-guess on ambiguity. 89 new tests: golden-file cross-checks of all 117 documents against the canonical ledger (dates, amounts, balances), plus API-level flow tests incl. PDF+OFX collapse. 124 total green. | Yes — parsers passed the full golden sweep on the first run. |
| 6 | Aug 9 | "build" (milestone 6) | Provider layer: FinancialDataProvider protocol, MockProvider (cursor pagination, simulated latency, 5% failures, 429s), documented PlaidProvider stub; sync service with retry/backoff, per-account cursors in sync_runs, statement-account adoption; SSE event stream; "Sync now"/status endpoints; labeled dev simulate control; demo-profile seed script. Live path proven: SSE stream received sync progress + transactions.new while injecting. 130 tests green. | Mostly — tests caught a real bug (sync duplicated statement-created accounts → added provider-key adoption, D-014) and a reporting flaw (balance failure discarded an account's ingested-row count, D-015). |
| 7 | Aug 9 | "build" (milestone 7) | Design system implemented: §13 tokens (light+dark, OS-following), Inter + IBM Plex Mono bundled locally, hairline-rule structure, focus rings, reduced-motion; five-state components (loading/empty/partial/error + populated pattern); Welcome/profile-select screen with strength meter; app shell with persistent left nav (scrollable top bar ≤ 900px), re-auth profile switching; honest NotBuilt placeholders for future-milestone pages; money formatter with integer-only math + 4 vitest tests. Screenshotted light/dark/mobile via Playwright and critiqued against §13. | Yes. |
| 8 | Aug 9 | "build" (milestone 8) | Dashboard (spending power = liquid − card balances, month-vs-month, needs-attention, recent activity, Sync-now with live SSE status), Accounts (grouped by institution, closed de-emphasized), Transactions (search/filters/paging, source indicator, inline category editing that writes user_corrections), Documents (drag-drop upload, per-file status, preview-before-import, account matching with create-from-statement, per-document transactions). System categories seeded via reversible data migration 0003. Live-update and full upload→preview→import flows proven in a real browser; import of the Nov statement showed "Imported 1, merged 25" — the 1 being the planted missing-in-provider check. 133 tests green. | Mostly — a TS type miss and a Playwright selector ambiguity; also the isolation test rightly flagged shared system categories, and its assertion was refined rather than the design changed. |
| 9 | Aug 9 | "build" (milestone 9) | LLMProvider protocol: OllamaProvider (loopback-guarded, schema-constrained JSON) + AnthropicProvider (off by default, labeled off-device); ai_calls logging; two-pass categorization (corrections → builtin rules → LLM batches of 15 with tier-2 few-shot from correction history); confidence <0.8 → review; hallucinated categories rejected; cryptic-descriptor confidence capped in code (D-017); Review queue screen with j/k/a/Shift+A keyboard flow and bulk apply; Settings screen (model status/choice, speed test, usage accounting, labeled dev tool, audit viewer, theme); tier-3 JSONL training capture. Installed REAL Ollama + qwen2.5:3b in the dev container and tuned the prompt against it — first version was overconfident on cryptic descriptors, rule-heavy prompt made the 3B erratic, final design keeps the prompt short and enforces the cap deterministically. 139 tests green. | Mostly — two prompt iterations against the real model, plus a test that initially under-ran the categorization limit. |
| 10 | Aug 9 | "build" (milestone 10) | Reconciliation engine: identity-based classification (statement-only/provider-only/both), amount-mismatch pairing, occurrence-aware duplicate detection, pre-resolved date-shift findings (merge now preserves the second source's date), provider-balance anchoring for computed period endings; LLM narration with deterministic template fallback; run/run-all/resolve endpoints + Reconciliation screen; dashboard findings count wired. Full-pipeline test: sync + all 117 documents imported + 79 periods reconciled — all 13 planted events detected, the only actionable findings are the 3 planted divergences (zero false positives), Nov delta exactly $230.00 explained by the missing check. Real-model narration verified in browser. 140 tests green. | Nearly — one assertion counted "DENVER" substrings and missed the two "DEN" descriptors. |
| 11 | Aug 9 | "build" (milestone 11, resumed after interruption) | Coach service with schema-driven tool loop over 5 user-scoped tools (query_transactions, get_account_balances, get_budget_targets, fetch_category_history, get_spending_summary); ≤4 calls enforced with forced honest summary; must-query-before-answering enforced; identical-call rejection; deterministic filter widening; sources (queries + transactions + computed totals) returned with every answer; Coach chat screen with "Queried total" line and expandable sources; degraded state without model. Tested on BOTH real local models: 3B exposed repeat-loops, filter give-up, and total misreporting (all mitigated deterministically, D-020); the 7B default answered the Denver-trip question correctly ($943.39, 1 tool call, 7 sources). 146 tests green. | Mostly — three real-model iterations; also the identical-call dedupe improvement broke (and improved) the cap test. |
| 12 | Aug 10 | "Build" (milestone 12) | Budgets: monthly targets per category (inline upsert/clear), actuals vs targets with over-target state, uncategorized remainder shown honestly. Simulator: deterministic projection from the real distribution over complete months only (mean + range per category, honest refusal without history), monthly delta and cumulative savings table, spending-power projection anchored on the live dashboard number; model explanation + second-order effects as language only, degrading silently. Verified in browser with the real 7B; its first explanation multiplied a total wrong, fixed by precomputing every figure it may mention (D-021). Budgets & Simulator screen; 150 tests green. | Mostly — one real-model prose-arithmetic slip, closed deterministically. |
| 13 | Aug 10 | "Build" (milestone 13) | Hardening pass: global 500 handler that logs the traceback server-side and never leaks internals (tested with an injected RuntimeError); unknown /api/* routes return 404 JSON instead of the SPA page; 15 MB upload cap with a specific message; startup loopback warning via lifespan; React ErrorBoundary (a crash is never a blank screen); skip-to-content link; per-page document titles; overflow containment. Required §17 vitest route sweep added: all 9 routes smoke-render against stubbed empty datasets (13 vitest total). 375px verified: zero horizontal overflow on the three data-heavy screens. 154 pytest green. | Nearly — the 500-handler test first tripped over Starlette's re-raise behavior; fixed by production-like raise_app_exceptions=False in the test client. |
| 14 | Aug 10 | "build" (milestone 14) | Packaging: stdlib-only launcher.py behind three thin OS launchers (version check, venv+pinned install keyed to requirements hash, migrations, idempotent seeding, Ollama detection that never blocks startup, port fallback, health poll, app-mode browser with fallbacks, graceful shutdown); scripts/build_zip.py staging exactly the runtime (prebuilt frontend, no Node) into dist/MeridianFinancial-v0.1.zip (0.9 MB) with executable bits preserved; full professional README. Clean-machine test from the zip passed end to end in ~40 s incl. the Ollama-absent degraded path; relaunch reuses the venv. | Nearly — first clean-room attempt killed its own shell with an overzealous pkill pattern. |
| 15 | Aug 10 | "build" (milestone 15) | Assignment deliverables: DEV_REPORT.md (assistant + approach, most helpful prompts, features, challenges, the local-model architecture evolution, App #1 vs #2 comparison, ~22 h total), SCREENSHOT_GUIDE.md (both required shots with visible-clock instructions per OS + a 90-second demo path), TIME_LOG totals. Final sweep: 154 pytest + 13 vitest green. All 15 milestones complete. | Yes. |
